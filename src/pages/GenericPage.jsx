export default function GenericPage({ title }) {
  return (
    <div className="page-generic container">
      <h1 className="page-generic__title">{title}</h1>
      <p className="page-generic__lead">This page is under construction. Content will match the live NZDCR site.</p>
    </div>
  )
}
